{
  "name": "jaeder-systems-root",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=22.0.0"
  },
  "workspaces": [
    "erp-james-unico"
  ],
  "scripts": {
    "build": "npm --workspace jaeder-systems run build",
    "build:standalone": "npm --workspace jaeder-systems run build:standalone",
    "validate": "npm --workspace jaeder-systems run validate",
    "validate:shell": "npm --workspace jaeder-systems run validate:shell",
    "validate:operations": "npm --workspace jaeder-systems run validate:operations",
    "validate:release": "npm --workspace jaeder-systems run validate:release"
  }
}
